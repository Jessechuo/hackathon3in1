# Secrets — API keys and mail passwords must NEVER reach this repo.
.env
.env.*
*.env
.env.txt

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# LLM response cache — local state, and large. Re-runs rebuild it.
.cache/

# Verdicts for mail received at runtime; pairs with mail/ below, which is
# not committed either. Committing it would put results in the dashboard
# for emails that do not exist on the server.
out/mail_results.json

# Organizer material — NEVER commit.
# sdoc-hackathon-docker/ contains ground_truth.json (the answer key).
sdoc-hackathon-docker/
zip/
Shipping Document Verification Use Case.pdf

# Vendored third-party skill data (regenerate: uipro init --ai claude)
.claude/

# Local scratch
.playwright-mcp/
shot-*.png

# Mail received at runtime - local state
/mail/
